<UserControl x:Class="Gemini.Demo.Modules.WebPage.Views.WebPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Gemini.Demo.Modules.WebPage.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <controls:InternetExplorer x:Name="Browser"
                               Target="{Binding Path=TargetUri, Mode=TwoWay}"
                               Title="{Binding Path=TitleOfPage, Mode=OneWayToSource}"
                               IsLoading="{Binding Path=IsPageLoading, Mode=OneWayToSource}"
                               Source="{Binding Path=LoadedUri, Mode=OneWayToSource}"/>
</UserControl>
