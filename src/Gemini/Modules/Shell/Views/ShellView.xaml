<Window x:Class="Gemini.Modules.Shell.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
		xmlns:ad="clr-namespace:AvalonDock;assembly=AvalonDock" 
		xmlns:controls="clr-namespace:Gemini.Framework.Controls"
        Title="{Binding Title}" Icon="{Binding Icon}" 
		Width="1000" Height="800">
	<Window.Resources>
		<ResourceDictionary Source="../Resources/Styles.xaml" />
	</Window.Resources>

	<DockPanel>
		<DockPanel.Resources>
			<ResourceDictionary Source="/AvalonDock.Themes;Component/Themes/Dev2010.xaml"/>
		</DockPanel.Resources>
		
		<controls:Menu ItemsSource="{Binding MainMenu}"
			  ItemTemplate="{StaticResource menuTemplate}" 
              DockPanel.Dock="Top" />

		<StatusBar DockPanel.Dock="Bottom">
			<StatusBarItem>
				<TextBlock Text="{Binding StatusBar.Message}" />
			</StatusBarItem>
		</StatusBar>

		<ad:DockingManager x:Name="Manager">
			<ad:ResizingPanel Orientation="Vertical">
				<ad:ResizingPanel Orientation="Horizontal">
					<ad:DockablePane x:Name="LeftPane" ad:ResizingPanel.ResizeWidth="300" />
					<ad:DocumentPane x:Name="DocumentPane"/>
					<ad:DockablePane x:Name="RightPane" ad:ResizingPanel.ResizeWidth="300" />
				</ad:ResizingPanel>
				<ad:DockablePane x:Name="BottomPane" ad:ResizingPanel.ResizeHeight="300" />
			</ad:ResizingPanel>
		</ad:DockingManager>
	</DockPanel>
</Window>